# Gradle
.gradle/
build/
app/build/

# Local configuration
local.properties

# Android Studio / IntelliJ
.idea/
*.iml
.DS_Store
captures/
.externalNativeBuild/
.cxx/

# Log/Cache
*.log
.navigation/

# Keystore (never commit signing keys)
*.jks
*.keystore

# Generated wrapper distribution cache (not the properties/scripts, just the
# downloaded Gradle distribution if it ever ends up inside the repo folder)
gradle/wrapper/dists/
